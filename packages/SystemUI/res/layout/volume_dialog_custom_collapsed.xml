<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/volume_dialog"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:padding="8dp">

    <LinearLayout
        android:id="@+id/volume_dialog_collapsed_container"
        android:layout_width="56dp"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <!-- Main Volume Slider Capsule -->
        <FrameLayout
            android:id="@+id/collapsed_slider_capsule"
            android:layout_width="56dp"
            android:layout_height="190dp"
            android:background="@drawable/hyperos_volume_capsule_bg">

            <!-- Vertical SeekBar -->
            <SeekBar
                android:id="@+id/collapsed_media_seekbar"
                android:layout_width="190dp"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:background="@null"
                android:paddingStart="0dp"
                android:paddingEnd="0dp"
                android:progressDrawable="@drawable/hyperos_vertical_progress_drawable"
                android:rotation="270"
                android:splitTrack="false"
                android:thumb="@null" />

            <!-- Three Dots Button at top (Expands dialog to 4 sliders + timers) -->
            <FrameLayout
                android:id="@+id/volume_expand_button"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_gravity="center_horizontal|top"
                android:layout_marginTop="6dp"
                android:clickable="true"
                android:focusable="true">
                <ImageView
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_volume_dots" />
            </FrameLayout>

            <!-- Media Volume Icon in lower middle -->
            <ImageView
                android:id="@+id/collapsed_media_icon"
                android:layout_width="26dp"
                android:layout_height="26dp"
                android:layout_gravity="center_horizontal|bottom"
                android:layout_marginBottom="24dp"
                android:src="@drawable/ic_volume_speaker_blue" />
        </FrameLayout>

        <!-- Separate Ringer Mode Capsule Button -->
        <FrameLayout
            android:id="@+id/collapsed_ringer_btn"
            android:layout_width="56dp"
            android:layout_height="44dp"
            android:layout_marginTop="10dp"
            android:background="@drawable/hyperos_volume_capsule_bg"
            android:clickable="true"
            android:focusable="true">
            <ImageView
                android:id="@+id/collapsed_ringer_icon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_volume_bell_blue" />
        </FrameLayout>

        <!-- Separate DND Capsule Button -->
        <FrameLayout
            android:id="@+id/collapsed_dnd_btn"
            android:layout_width="56dp"
            android:layout_height="44dp"
            android:layout_marginTop="10dp"
            android:background="@drawable/hyperos_volume_capsule_bg"
            android:clickable="true"
            android:focusable="true">
            <ImageView
                android:id="@+id/collapsed_dnd_icon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_volume_moon_white" />
        </FrameLayout>

    </LinearLayout>

    <!-- Hidden dynamic container for standard row binding compatibility -->
    <LinearLayout
        android:id="@+id/volume_dialog_rows"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

</FrameLayout>
